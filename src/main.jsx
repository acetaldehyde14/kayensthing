import React from "react";
import ReactDOM from "react-dom/client";
import HandwritingAnalysisApp from "../HandwritingAnalysisApp.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <HandwritingAnalysisApp />
  </React.StrictMode>
);
